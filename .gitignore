# ── Python embebido local (muy pesado, NO subir a GitHub) ──
mask/python/
python/

# ── Modelo pesado .h5 (se reemplaza con el .tflite liviano) ──
mask_detector_trained.h5
mask/mask_detector_trained.h5
mask_detector_trained.h5.part1
mask_detector_trained.h5.part2
mask_detector_trained.h5.part3
mask/mask_detector_trained.h5.part1
mask/mask_detector_trained.h5.part2
mask/mask_detector_trained.h5.part3

# ── El modelo .tflite SÍ se sube a GitHub (73 MB, bajo límite) ──
# mask_detector_trained.tflite  <-- NO ignorar

# ── Caché de Python ──
**/__pycache__/
*.pyc
*.pyo
*.pyd

# ── Logs ──
*.log
predictions.log

# ── Entornos virtuales ──
venv/
.venv/
env/
.env

# ── Sistema y editores ──
.vscode/
.idea/
Thumbs.db
.DS_Store

# ── Archivos de documentación y scripts locales ──
Informe_Sistema_Detector_Mascaras.docx
